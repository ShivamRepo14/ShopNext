{
  "version": 2,
  "builds": [
    {
      "src": "frontend/mern/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "frontend/mern/build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "frontend/mern/$1"
    }
  ]
}
